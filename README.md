# 传感器与智能检测

本项目采用 MIT License 开源协议。详细内容见 [LICENSE](LICENSE)。

## 第0章源代码

1. 傅里叶变换与频域分析
2. 拉普拉斯变换与频域分析
3. Z变换与频域分析

## 第三章源代码

1. 相机标定
2. 常用的图像滤波算法
3. 噪声与平滑
4. 基于背景减除法的动态车辆荷载监测

## 第十章源代码

包含7个完整代码示例，所有代码均已验证可运行：

1. **移动平均去噪**：复现教材例10-1，SMA（n=3）与EMA（α=0.2）对比可视化
2. **卡尔曼滤波降噪**：一维卡尔曼滤波完整实现，含预测与校正两步递推
3. **小波变换降噪**：PyWavelets库实现db4小波软阈值去噪三步流程
4. **基于分布的异常值诊断**：复现例10-2，3σ准则与箱形图IQR方法，输出 `outlier=[7.8]`
5. **基于聚类的异常值诊断**：DBSCAN识别二维监测数据中的4个离群点
6. **拉格朗日插值补全**：复现例10-3，`L(6) = 3.4667`，与教材完全一致
7. **压缩感知稀疏重构**：基于L1正则化的频域稀疏信号恢复（简化版，MSE=0.0019）

**运行环境依赖**：`numpy`, `pandas`, `matplotlib`, `pywavelets`, `scikit-learn`, `scipy`（均为标准Python科学计算库）

---

## 第十一章源代码

包含5个完整代码示例，所有代码均已验证可运行：

1. **基本统计分析**：复现例11-1，计算均值3.02、几何均值2.77、中位数2.55、方差2.93、标准差1.71、极差5.8、变异系数56.66%
2. **概率密度函数估计**：正态分布拟合、GMM双峰拟合、核密度估计(KDE)、Welch平均周期图功率谱密度，四图并排对比
3. **极值分析**：GEV区间极值法与GPD过阈法拟合尾部分布，可视化对比
4. **相关性分析**：复现例11-3 Pearson相关（r=-0.968，挠度随车流增大而减小，这个应该是教材所给的数据有误），以及典型相关分析(CCA)两对典型变量
5. **回归分析**：复现例11-4一元线性回归（y≈0.046x+0.567），并与二次多项式回归对比，散点图可视化

**运行环境依赖**：`numpy`, `scipy`, `scikit-learn`, `matplotlib`（均为标准Python科学计算库）


---

## 第十二章源代码

包含5个完整代码模块，所有代码均已验证可运行：

1. **聚类方法**：层次聚类树状图（Dendrogram）+ k-Means散点图（k=3）
2. **树模型**：决策树（Gini指标，max_depth=3）可视化结构图 + 随机森林（100棵树），准确率均达90%
3. **支持向量机**：线性SVM与RBF核SVM决策边界对比可视化
4. **神经网络与深度学习**：MLP（ReLU激活，双隐藏层）训练损失曲线 + PyTorch定义CNN/LSTM网络结构（含层参数打印）
5. **XGBoost集成对比**：在相同数据集上对比决策树、随机森林、SVM、MLP、XGBoost五种算法，使用Seaborn绘制横向柱状图

**运行环境依赖**：`numpy`, `pandas`, `matplotlib`, `seaborn`, `scikit-learn`, `scipy`, `torch`, `xgboost`


---

## 第十五章源代码

包含两大模块，代码已全部验证可运行：

**模块一：基于统计时间序列的损伤识别**
- 模拟健康/损伤两种状态下的AR(2)振动响应信号
- 使用`statsmodels.ARIMA`拟合各时间窗口的AR系数
- PCA降维提取一维损伤特征
- 绘制X-bar控制图，第11个窗口起特征点超出UCL，成功实现损伤判定

**模块二：基于智能优化算法的损伤识别（复现例15-1）**
- 构建三自由度模型目标函数 J(α) = ‖ω(α)−ωᵈ‖²
- **GA**（`scikit-opt`）：识别 α₃≈0.165，目标函数≈4.89e-3
- **PSO**（`scikit-opt`）：识别 α₃=0.2000，目标函数≈1.89e-15（精度极高）
- **SA**（`scikit-opt`）：识别 α₃=0.2000，目标函数≈1.87e-11
- **ACO**（自定义连续离散化实现）：识别 α₃≈0.131，目标函数≈1.90e-2
- 绘制四种算法收敛曲线对比图（对数坐标）

**运行依赖**：`numpy`, `scipy`, `matplotlib`, `statsmodels`, `scikit-opt`, `scikit-learn`（均可通过`pip install`安装）